// Print all primes up to the largest you can represent in your language.

public class PrimeNumbers {
  public static void main(String []arges) {
    int maxValue = 10;//Integer.MAX_VALUE;
    for (int p = 1; p <= maxValue; p++) {
      for (int m = 2; m < p; m++) {
      }
    }
  }
}
